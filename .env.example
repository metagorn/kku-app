# Copy to `.env` and fill values. Public only — do NOT put secrets.

# Classroom API base URL (default already set in code)
EXPO_PUBLIC_API_BASE_URL=https://cis.kku.ac.th/api/classroom

# Optional API key header (if your server requires ApiKeyAuth)
# Sent as: x-api-key: <value>
EXPO_PUBLIC_API_KEY=YOUR_API_KEY_HERE

